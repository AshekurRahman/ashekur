{
  "slug": "rest-api-not-working",
  "category": "woocommerce",
  "subcategory": "troubleshooting",
  "question": "Why Isn't the WooCommerce REST API Responding Correctly?",
  "metaDescription": "Permalink structure, API key permissions, and a security plugin blocking REST requests are the three most common causes of a broken integration.",
  "readTime": "3 min read",
  "datePublished": "2026-09-07",
  "dateModified": "2026-09-07",
  "shortAnswer": "<p>The WooCommerce REST API depends on WordPress's permalink structure being set to anything other than \"Plain\" (which breaks REST routing entirely), correctly scoped API key permissions (read, write, or both) matching what the integration actually needs, and no security plugin or server-level firewall blocking REST API requests specifically. Checking these three in order resolves most \"API not working\" cases.</p>",
  "image": {
    "alt": "A REST API request failing due to permalink structure, insufficient key permissions, or a security plugin blocking the endpoint",
    "prompt": "Create a clean, professional technical illustration of an API request being blocked at one of three checkpoints: routing, permission, and firewall. Dark navy background, red and indigo accent colors, flat minimal geometric style, no text, no logos, no people."
  },
  "bodyHtml": "<h2 id=\"why-this-happens\">Why This Happens</h2>\n<p>WordPress's REST API (which WooCommerce's own API builds on) relies on proper URL rewriting to route requests correctly, which requires a permalink structure other than the default \"Plain\" setting. Beyond that, WooCommerce's API keys carry specific, explicitly granted permissions, and various security tools (firewalls, security plugins) sometimes block REST API traffic by default as a blanket security measure, unaware that legitimate integrations depend on it.</p>\n\n<h2 id=\"common-causes\">Common Causes</h2>\n<ul>\n<li><strong>Permalink structure set to \"Plain\"</strong> under Settings &rarr; Permalinks, which breaks REST API routing entirely regardless of anything else being configured correctly.</li>\n<li><strong>API key permissions insufficient</strong> for the specific operation being attempted &mdash; a read-only key can't be used for a write/update request, which will fail with an authorization error rather than a generic one.</li>\n<li><strong>A security plugin or web application firewall</strong> blocking REST API endpoints, sometimes as an overly broad default rule not intended to catch legitimate WooCommerce API use.</li>\n<li><strong>Expired or revoked API keys,</strong> particularly if keys were regenerated at some point without updating the integration using the old ones.</li>\n<li><strong>A caching layer</strong> incorrectly caching API responses that should be dynamic, causing stale or unexpected data to be returned.</li>\n</ul>\n\n<h2 id=\"how-to-fix-it\">How to Diagnose and Fix It</h2>\n<ol>\n<li><strong>Confirm permalinks aren't set to \"Plain\"</strong> first, since this alone can explain a complete API failure.</li>\n<li><strong>Check the specific API key's permissions</strong> against what the failing request actually needs.</li>\n<li><strong>Test the API request directly</strong> (via a tool like Postman) outside of the integrating application, to isolate whether the issue is server-side or specific to the integration's own code.</li>\n<li><strong>Temporarily disable security plugins on staging</strong> to test whether one is blocking REST API traffic.</li>\n</ol>\n<p>Need a broken API integration diagnosed and fixed? See <a href=\"../../../../services/woocommerce-development/\">custom WooCommerce development</a>.</p>",
  "hasCode": false
}
