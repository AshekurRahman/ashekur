{
  "slug": "what-is-tokenization",
  "category": "woocommerce",
  "subcategory": "payments",
  "question": "What Does \"Tokenization\" Actually Mean for Stored Card Data?",
  "metaDescription": "A saved card is really just a random reference string the gateway can charge later — the actual card number is never stored on your site at all.",
  "readTime": "2 min read",
  "datePublished": "2026-09-07",
  "dateModified": "2026-09-07",
  "shortAnswer": "<p>Tokenization replaces a customer's actual card number with a random, meaningless reference string (the \"token\") that only the gateway can map back to the real card. When WooCommerce shows a saved card at checkout, it's really just storing and displaying that token, plus a few safe display details like the last four digits and expiry &mdash; the full card number itself is never stored on your site's database at all, which is exactly what makes this approach so much safer.</p>",
  "image": {
    "alt": "A customer's actual card number being replaced by a random, meaningless token that only the payment gateway can map back to the real card",
    "prompt": "Create a clean, professional technical illustration of a real card number transforming into an abstract random token string, stored separately from the actual card details which remain locked in the gateway's own vault. Dark navy background, indigo and teal accent colors, flat minimal geometric style, no text, no logos, no people."
  },
  "bodyHtml": "<h2 id=\"how-it-works\">How Tokenization Actually Works</h2>\n<p>When a customer enters a card at checkout with \"save this card\" selected, the card details go directly from their browser to the gateway's secure servers (never touching your site's own server if using hosted or embedded fields, as most modern integrations do). The gateway generates a token &mdash; essentially a random ID with no mathematical relationship to the actual card number &mdash; and sends that token back to WooCommerce to store. Next time the customer checks out, WooCommerce sends the token back to the gateway, which looks up the real card on its own secure side and processes the charge.</p>\n\n<h2 id=\"common-causes\">Why This Matters</h2>\n<ul>\n<li><strong>A database breach of the store's own site</strong> exposes only tokens, which are useless to an attacker without access to the specific gateway account that issued them &mdash; this is the core security benefit tokenization provides.</li>\n<li><strong>PCI compliance burden drops significantly</strong> for stores using tokenization through hosted/embedded gateway fields, since the site itself never handles raw card data at any point.</li>\n<li><strong>Tokens are typically gateway-specific,</strong> which is exactly why saved cards don't carry over when switching payment gateways &mdash; the old token means nothing to a different provider.</li>\n<li><strong>The last-four-digits and expiry shown at checkout</strong> are safe display details the gateway explicitly permits storing alongside the token, since they don't expose enough to be useful for fraud on their own.</li>\n</ul>\n\n<h2 id=\"how-to-fix-it\">What This Means Practically</h2>\n<ol>\n<li><strong>Confirm the active gateway integration uses proper tokenization</strong> (virtually all reputable ones do) rather than any custom handling of raw card numbers.</li>\n<li><strong>Never attempt to store or log full card numbers anywhere on the site,</strong> even temporarily, regardless of tokenization being used elsewhere.</li>\n<li><strong>Understand that saved cards are gateway-specific</strong> when planning any future gateway migration.</li>\n</ol>\n<p>Questions about how your specific gateway handles stored payment data? See <a href=\"../../../../services/woocommerce-fix/\">WooCommerce fixes</a>.</p>",
  "hasCode": false
}
