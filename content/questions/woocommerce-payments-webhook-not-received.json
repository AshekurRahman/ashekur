{
  "slug": "webhook-not-received",
  "category": "woocommerce",
  "subcategory": "payments",
  "question": "Why Isn't My Payment Gateway's Webhook Reaching WooCommerce?",
  "metaDescription": "Usually the endpoint URL is misconfigured, blocked by a firewall/security plugin, or pointing at the wrong environment (test vs. live).",
  "readTime": "3 min read",
  "datePublished": "2026-09-07",
  "dateModified": "2026-09-07",
  "shortAnswer": "<p>Gateway webhooks are just HTTP requests the gateway's servers send to a specific WooCommerce endpoint URL, so anything that blocks or misroutes ordinary incoming requests can block them too &mdash; a misconfigured or mistyped endpoint URL in the gateway's dashboard, a firewall or security plugin blocking the gateway's IP ranges, or a webhook still pointing at a test-mode endpoint after switching to live are the most common causes.</p>",
  "image": {
    "alt": "A payment gateway's webhook request being blocked before reaching WooCommerce's endpoint, due to a firewall or misconfigured URL",
    "prompt": "Create a clean, professional technical illustration of a webhook signal traveling from a gateway server toward a WooCommerce endpoint, blocked by a firewall/shield icon partway there. Dark navy background, red and indigo accent colors, flat minimal geometric style, no text, no logos, no people."
  },
  "bodyHtml": "<h2 id=\"how-it-works\">Why Webhooks Matter</h2>\n<p>Webhooks let the gateway notify WooCommerce asynchronously about events that happen outside the immediate checkout request &mdash; a delayed payment confirmation, a dispute being opened, a subscription renewal succeeding on the gateway's own schedule. When a webhook fails to arrive, WooCommerce simply never finds out about that event, which can leave orders stuck in an incorrect status indefinitely rather than producing an obvious error.</p>\n\n<h2 id=\"common-causes\">Common Causes</h2>\n<ul>\n<li><strong>The webhook endpoint URL configured in the gateway's dashboard is incorrect,</strong> outdated after a domain change, or was never actually set up during initial gateway configuration.</li>\n<li><strong>A firewall, security plugin, or hosting-level bot protection</strong> blocking the gateway's server IP ranges, mistaking legitimate webhook traffic for an attack &mdash; this is a very common and often overlooked cause.</li>\n<li><strong>A webhook still configured for test/sandbox mode</strong> after the store switched to live, or vice versa, sending events to an endpoint that isn't listening in the current mode.</li>\n<li><strong>A caching plugin intercepting the webhook request</strong> and returning a cached response, rather than letting it reach WooCommerce's actual endpoint handler.</li>\n<li><strong>A webhook signature verification failure</strong> silently rejecting genuine requests, if API keys or a webhook signing secret were rotated on one side but not updated on the other.</li>\n</ul>\n\n<h2 id=\"how-to-fix-it\">How to Diagnose and Fix It</h2>\n<ol>\n<li><strong>Check the gateway's own webhook delivery log</strong> (most dashboards show delivery attempts and their response codes) to see exactly what happened on send.</li>\n<li><strong>Confirm the endpoint URL is correct and matches the current mode</strong> (test vs. live) the gateway is running in.</li>\n<li><strong>Temporarily disable firewall/security plugins</strong> on a staging copy to test whether one is blocking the request.</li>\n<li><strong>Verify any signing secret or API key used for webhook verification</strong> matches on both sides if signature validation is failing.</li>\n<li><strong>Exclude the webhook endpoint from caching</strong> if a caching plugin is active and might be intercepting the request.</li>\n</ol>\n<p>Orders stuck in the wrong status because of missed webhooks? See <a href=\"../../../../services/woocommerce-fix/\">WooCommerce fixes</a>.</p>",
  "hasCode": false
}
