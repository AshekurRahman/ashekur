{
  "slug": "minimum-order-amount-method",
  "category": "woocommerce",
  "subcategory": "payments",
  "question": "Can I Set a Minimum Order Amount for a Specific Payment Method?",
  "metaDescription": "Not through core WooCommerce settings alone — this needs a small code snippet or a dedicated plugin, since minimums there apply store-wide.",
  "readTime": "3 min read",
  "datePublished": "2026-09-07",
  "dateModified": "2026-09-07",
  "shortAnswer": "<p>Not directly through WooCommerce's own settings, which only support a single store-wide minimum/maximum order amount if one is set at all, without any per-gateway distinction. Restricting a specific payment method to orders above (or below) a certain amount &mdash; a common request for methods with high per-transaction fees, like some buy-now-pay-later options &mdash; needs either a small custom filter or a dedicated plugin built for conditional payment method availability.</p>",
  "image": {
    "alt": "A specific payment method becoming available or unavailable at checkout depending on whether the order total crosses a set threshold",
    "prompt": "Create a clean, professional technical illustration of a payment method icon appearing or fading based on an order total gauge crossing a marked threshold line. Dark navy background, indigo and amber accent colors, flat minimal geometric style, no text, no logos, no people."
  },
  "bodyHtml": "<h2 id=\"how-it-works\">How to Actually Do This</h2>\n<p>WooCommerce provides a filter (<code>woocommerce_available_payment_gateways</code>) specifically for conditionally showing or hiding gateways based on any logic, including the current cart total. A small snippet checking <code>WC()-&gt;cart-&gt;get_total()</code> against a threshold and unsetting the specific gateway when the condition isn't met is the standard approach, and several \"conditional payment gateways\" plugins package this exact logic with a settings UI instead of custom code.</p>\n<pre class=\"language-php\"><code class=\"language-php\">add_filter( 'woocommerce_available_payment_gateways', function( $gateways ) {\n    if ( isset( $gateways['bnpl_gateway'] ) && WC()-&gt;cart &amp;&amp; WC()-&gt;cart-&gt;get_total( 'edit' ) &lt; 50 ) {\n        unset( $gateways['bnpl_gateway'] );\n    }\n    return $gateways;\n} );</code></pre>\n\n<h2 id=\"common-causes\">Things Worth Getting Right</h2>\n<ul>\n<li><strong>Use the cart total in the correct format</strong> (a raw number via <code>get_total( 'edit' )</code>, not the formatted display string), since comparing against a formatted price string with currency symbols will silently fail.</li>\n<li><strong>Decide whether the threshold applies to the subtotal or the full total</strong> including tax and shipping, since this changes the comparison meaningfully for some order combinations.</li>\n<li><strong>Test on both the cart page and checkout page,</strong> since some themes/page builders render available gateways slightly differently, and the filter needs to apply consistently across both.</li>\n<li><strong>Consider messaging</strong> for the customer, since a payment method silently disappearing below a threshold can be confusing without some explanation (a note near the payment section, for instance).</li>\n</ul>\n\n<h2 id=\"how-to-fix-it\">Implementing It Properly</h2>\n<ol>\n<li><strong>Confirm the exact threshold and which gateway(s) it applies to</strong> before writing or configuring anything.</li>\n<li><strong>Use the <code>woocommerce_available_payment_gateways</code> filter</strong> (custom snippet or plugin) rather than trying to hide the option purely with CSS, which wouldn't actually prevent its use.</li>\n<li><strong>Add a brief note near checkout</strong> explaining the condition, so customers aren't confused by a missing option.</li>\n</ol>\n<p>Need this built and tested properly for a specific gateway setup? See <a href=\"../../../../services/woocommerce-development/\">custom WooCommerce development</a>.</p>",
  "hasCode": true
}
