{
  "slug": "nonce-security-tokens-explained",
  "category": "wordpress",
  "question": "What Is a Nonce in WordPress, and Why Do Forms Need One?",
  "metaDescription": "A short-lived security token confirming a request genuinely came from the expected page — it prevents a class of attack called cross-site request forgery.",
  "readTime": "2 min read",
  "datePublished": "2026-09-08",
  "dateModified": "2026-09-08",
  "shortAnswer": "<p>A nonce (\"number used once,\" despite technically being reusable within its short validity window) is a security token embedded in a form or AJAX request, verified on submission to confirm the request genuinely originated from the expected page and user session, rather than being forged by a malicious site tricking a logged-in user's browser into submitting an unintended action. This defends specifically against cross-site request forgery (CSRF), a well-known class of web attack.</p>",
  "image": {
    "alt": "A form submission verified against a security nonce token, confirming it genuinely came from the expected page rather than a forged request",
    "prompt": "Create a clean, professional technical illustration of a form submission being checked against an embedded security token before being accepted as genuine. Dark navy background, indigo and teal accent colors, flat minimal geometric style, no text, no logos, no people."
  },
  "bodyHtml": "<h2 id=\"how-it-works\">How Nonces Protect Against CSRF</h2>\n<p>Without this protection, a malicious website could craft a hidden form or request that, if a logged-in WordPress user's browser happened to visit it, would silently submit an action to the WordPress site using that user's existing authenticated session &mdash; deleting content, changing settings, anything the form's target action could do. A nonce, generated specifically for that user's session and that specific action, defeats this because the malicious site has no way to know or predict the correct nonce value to include.</p>\n\n<h2 id=\"common-causes\">Why This Sometimes Causes Visible Errors</h2>\n<ul>\n<li><strong>Nonces expire after a limited time</strong> (commonly around 24 hours, though effectively shorter in practice for a form left open), so a form submitted long after the page loaded can fail verification, producing a \"security check failed\" style error &mdash; covered in more detail for the WooCommerce checkout context elsewhere.</li>\n<li><strong>A cached page serving a stale, already-expired nonce</strong> is a common cause of this error appearing more often than expected, since the embedded nonce was generated at the time the page was originally rendered and cached, not when the visitor actually loaded it.</li>\n<li><strong>Custom AJAX functionality</strong> built without proper nonce verification is a real, common security gap in custom development, worth checking for in any custom form or AJAX handler.</li>\n</ul>\n\n<h2 id=\"how-to-fix-it\">Working With Nonces Correctly</h2>\n<ol>\n<li><strong>Always include and verify a nonce</strong> in any custom form or AJAX handler that performs a meaningful action, using WordPress's built-in <code>wp_nonce_field()</code> and <code>check_admin_referer()</code>/<code>wp_verify_nonce()</code> functions.</li>\n<li><strong>Exclude forms and AJAX-heavy pages from page caching,</strong> since a cached, stale nonce is a common cause of confusing verification failures.</li>\n<li><strong>If seeing \"security check failed\" errors frequently,</strong> check caching configuration first, as covered elsewhere in more detail.</li>\n</ol>\n<p>Need custom form/AJAX functionality built with proper security? See <a href=\"../../../services/wordpress-bug-fix/\">WordPress bug fix</a>.</p>",
  "hasCode": false
}
