{
  "slug": "restrict-shipping-countries",
  "category": "woocommerce",
  "subcategory": "payments",
  "question": "Can I Restrict a Payment Method to Specific Shipping Countries?",
  "metaDescription": "Not through core settings — WooCommerce restricts gateways by billing country only, so a shipping-based restriction needs a small custom filter.",
  "readTime": "3 min read",
  "datePublished": "2026-09-07",
  "dateModified": "2026-09-07",
  "shortAnswer": "<p>WooCommerce's built-in per-gateway country restriction filters by billing country, not shipping country, which matters for stores where the two differ (a gift order, for instance, or a business billing address separate from a delivery address). Restricting by shipping destination specifically &mdash; useful for a local-only payment method that shouldn't be offered on international deliveries &mdash; needs a small custom filter rather than a built-in setting.</p>",
  "image": {
    "alt": "A payment method becoming available or unavailable based on the shipping destination rather than the billing address",
    "prompt": "Create a clean, professional technical illustration of a payment method icon gated by a shipping destination map pin, distinct from a separate billing address icon. Dark navy background, indigo and teal accent colors, flat minimal geometric style, no text, no logos, no people."
  },
  "bodyHtml": "<h2 id=\"how-it-works\">Why This Needs Custom Code</h2>\n<p>Most gateway plugins' own \"restrict to specific countries\" setting checks the customer's billing address, since that's what the gateway itself typically cares about for fraud and compliance purposes. Filtering by shipping address instead requires hooking into the same <code>woocommerce_available_payment_gateways</code> filter used for other conditional-payment scenarios, checking the cart's shipping destination specifically.</p>\n<pre class=\"language-php\"><code class=\"language-php\">add_filter( 'woocommerce_available_payment_gateways', function( $gateways ) {\n    $shipping_country = WC()-&gt;customer ? WC()-&gt;customer-&gt;get_shipping_country() : '';\n    if ( isset( $gateways['local_only_gateway'] ) &amp;&amp; $shipping_country !== 'GB' ) {\n        unset( $gateways['local_only_gateway'] );\n    }\n    return $gateways;\n} );</code></pre>\n\n<h2 id=\"common-causes\">Things Worth Getting Right</h2>\n<ul>\n<li><strong>The shipping address may not be set yet</strong> at certain points in the checkout flow (before the customer has entered it), so the filter needs to handle an empty/unknown shipping country gracefully rather than assuming it's always available.</li>\n<li><strong>\"Ship to billing address\" toggles</strong> some checkout flows offer mean the shipping and billing country are sometimes identical anyway &mdash; worth confirming the actual customer behavior before assuming this distinction matters as much as it might in theory.</li>\n<li><strong>Cache and AJAX refresh behavior</strong> &mdash; WooCommerce needs to re-evaluate available gateways when the shipping address changes at checkout, which usually happens automatically via the checkout's own AJAX update, but is worth testing specifically.</li>\n</ul>\n\n<h2 id=\"how-to-fix-it\">Implementing It</h2>\n<ol>\n<li><strong>Confirm the exact restriction logic needed</strong> (which gateway, which countries) before writing the filter.</li>\n<li><strong>Use <code>woocommerce_available_payment_gateways</code></strong> checking shipping country via <code>WC()-&gt;customer-&gt;get_shipping_country()</code>, rather than the gateway's own billing-based setting.</li>\n<li><strong>Test that the gateway list updates correctly</strong> when the shipping address changes at checkout, without a full page reload.</li>\n</ol>\n<p>Need this built and tested properly? See <a href=\"../../../../services/woocommerce-development/\">custom WooCommerce development</a>.</p>",
  "hasCode": true
}
